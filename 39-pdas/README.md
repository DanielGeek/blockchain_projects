# Rust Solana Tutorial - Program Derived Addresses (PDAs)

# Used Commands
`solana config set --url localhost`
`solana config get`
`solana-test-validator`
`anchor build`
`anchor deploy`
`anchor test --skip-local-validator`